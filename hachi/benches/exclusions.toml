# Mirrored items that deliberately have no benchmark.
#
# `make bench-check` requires every item in `hachi/src` whose docstring carries a
# ``Mirrors `<ArkLib name>` `` line to be either benched -- a `// @covers` marker
# on a `bench_case!` -- or listed here. Silence is not an exclusion: an operation
# nobody measures is one the optimization loop cannot notice a regression in, and
# "we forgot" and "it is not worth measuring" must not look the same from the
# outside.
#
# The bar for a line here is that a criterion run on the item would measure the
# *harness* rather than the item: a type with no runtime, or straight-line code
# with no loop and no allocation. It is NOT "this seemed slow to benchmark" and
# it is NOT "this is not on the hot path" -- `commit::l1_norm` is nobody's hot
# path and it is benched. If an item does real work at any size, bench it.
#
# One signed-off exception class exists alongside that bar (the "infeasible at
# [NOZ26] Fig. 9 scale" section below): operations whose cost is pinned by the
# `params.rs` constants to hours per criterion iteration under schoolbook
# `ring::mul`. That is a *policy* exception -- explicitly not "seemed slow" but
# "provably cannot complete" -- decided by the user on 2026-08-28
# (the paper-parameters plan, Decision 3) and recorded in NOTES.md ("Adopting the
# paper's parameters"). Every entry in that section names the arithmetic that
# makes it infeasible and the condition for its removal: a sub-quadratic
# `ring::mul` champion landing.
#
# Each entry must say *why*, in a form that can be checked by reading the item.
#
# The `const`s in `hachi/src/params.rs` are absent from this file on purpose:
# they carry no `Mirrors` marker, because a const is a *value chosen for a
# generic ArkLib parameter* rather than a translation of a definition
# (`RING_LOG_DEGREE` is the argument `α` of `hachiModulus`, not `hachiModulus`).
# Never mirrored, so never owed a bench, so nothing to excuse. `params` is
# consequently the one module in `harness.py`'s MODULES with no `[[bench]]`
# target -- a claim that was briefly false, when `endpiece.rs` had no target of
# its own and its two items were benched from `benches/ringswitch.rs`. That
# arrangement is now a `coverage --strict` failure in its own right
# (`covered_paths`, `_one_control_per_binary`), so the claim is checked rather
# than merely written down: a module with mirrored items has a bench binary, or
# its items are listed below.

[exclusions]

# -- types -------------------------------------------------------------------
#
# A struct declaration has no runtime, and `rustitems.benchable()` excludes
# `struct` outright, so these can never be benched even in principle: a marker
# on a type always implies a line here. The operations over each type are
# benched individually.

"ring::Rq" = "a type declaration, not code -- the negacyclic ring's fourteen operations are benched separately"
"linalg::PolyVec" = "a type declaration, not code; its operations are benched separately"
"linalg::PolyMatrix" = "a type declaration, not code; `mat_vec_mul` is where its runtime is"
"commit::PublicParams" = "a type declaration, not code -- a pair of gadget matrices; building it is `PublicParams::new`, which mirrors nothing"
"commit::Decomp" = "a type declaration, not code -- the decomposition record `generate_decomps` produces, and that is the benched item"
"commit::Opening" = "a type declaration, not code -- the opening record `verify_weak` consumes, and that is the benched item"
"evalsplit::MlPoly" = "a type declaration, not code -- the monomial-coefficient vector; its reshape and evaluation are benched separately"
"evalsplit::MlEvals" = "a type declaration, not code -- the hypercube-value vector; its reshape and evaluation are benched separately"

# -- O(1) by inspection ------------------------------------------------------
#
# Straight-line code: no loop, no allocation, no call that has either. A case
# over one of these would time the `black_box` around it. Both bodies are short
# enough to check the claim by reading them.

"ring::Rq::coeff" = """
one bounds compare and one index (`hachi/src/ring.rs`): `if k < self.0.len()
{ self.0[k] } else { Fp::ZERO }`. No loop, no allocation. It is also measured
through every operation above it -- `mul` alone performs `N²` coefficient
reads -- so a regression here could not hide."""

"commit::centered_abs" = """
one shift, one compare, one subtraction on a single `Fp` (`hachi/src/commit.rs`):
`q / 2` folds at compile time because `Q` is a `const`, leaving
`if v <= half { v } else { q - v }`. Per-coefficient work with no loop of its
own; it is measured at scale through `l1_norm`, `l_infty_norm` and
`l2_norm_sq`, all three of which are benched."""

"evalsplit::split_equiv" = """
one `usize` multiplication by a const and one addition
(`hachi/src/evalsplit.rs`): `y + ML_LOW_LEN * x`. No loop, no allocation; it is
also measured through both reshapes, which call it per entry."""

"evalsplit::split_equiv_inv" = """
one `usize` division and one remainder by a const
(`hachi/src/evalsplit.rs`): `(k / ML_LOW_LEN, k % ML_LOW_LEN)`. No loop, no
allocation; measured through `to_polynomial`, which calls it per entry."""

# -- infeasible at [NOZ26] Fig. 9 scale (signed-off policy exception) ---------
#
# See the header for the bar this section is an exception to, the sign-off, and
# the removal condition. Common arithmetic: one schoolbook `ring::mul` at
# `RING_DEGREE = 1024` is `2^20` field operations (~2-4 ms); one message block
# is `MESSAGE_ROWS · GADGET_DIGITS = 8192` ring elements wide, so anything that
# multiplies through the inner matrix `A` (`1 × 8192`) pays >= 8192 ring muls
# (~tens of seconds) *per block*, at any block count -- and the scheme has
# `BLOCKS = 1024` of them. The case bodies are retained (unregistered) in the
# bench files, and the semantics they exercised run on demand via the
# `#[ignore]`d full-const tests (`cargo test --release -- --ignored`).

"chain::chain_verify" = """
The composed verifier: seven statement maps and the three boolean checks the
specification's verifiers run (`roundCheck` per round, `finalCheck`,
`endPieceCheck`). Two walls, and it is worth saying which are *not* here: no
`paper_rel_out` (Eq. (20) is the relation `relOut`, never evaluated by a
verifier) and no `2^m0` witness table (the prover's, in `chain_open`). What
remains is `rlin_stmt` -- the `R^lin` matrix, 2.2 GiB at `mu0 = 57 344` -- and
`final_check`'s `alpha_public_table`, `2^m0` extension elements at
`M_ZERO = 26`. Infeasible as a criterion row; runnable as a test, which is what
`tests/chain_semantics.rs` does at a toy cube.

Every row that *can* be measured is measured under its own name; what a row
here would add is the threading, which is statement maps and no arithmetic.

Removal condition (restated 2026-09-11, measured): a row here must *accept*,
or `&&` and the round loop's early `None` leave `final_check` and the end piece
untimed; accepting needs a covering cube `(mu + n*delta)*d <= 2^m0`, and the
honest side sizes its widths from `params` (`honest_z`, `rel_out`), so the
smallest accepting shape is the pin's `m0 = 26`. The walls at that shape, each
with its own removal: `alpha_public_table` (2^26 entries x 5 rows x a
1024-coefficient evaluation, hours; removed by the tensor split of `A~`,
target-5 brief S6), `rlin_stmt` (2.2 GiB, removed by a lazy entry-at-alpha
form), `lift_commit` (`ring::mul`'s width). When those land, this row returns
as the composed verifier at the pin; until then the end-to-end number is
Stage 7's span comparison against `logs/paper-impl/`."""

"chain::chain_open" = """
The honest prover: `honest_compute_v` (a `2^23`-order commitment),
`lift_commit`, `honest_round_messages` (the `2^m0` witness table folded through
`M_ZERO = 26` rounds of `honest_compute_g`) and `honest_compute_y`. Each is
excluded under its own name for its own reason; this composes them and adds
nothing timeable. Same removal condition as `chain_verify`."""

"quadeval::rlin_cw" = """
`blocks * messageDigits`: one `usize` product. O(1) by inspection, the
`params.rs`-constant class of the header. The four `rlinC*` siblings and
`rlin_rows` are the same."""

"quadeval::rlin_ct" = """
`blocks * (innerRows * innerDigits)`: two `usize` products. O(1) by
inspection."""

"quadeval::rlin_cz" = """
`messageRows * messageDigits * zDigits`: two `usize` products. O(1) by
inspection."""

"quadeval::rlin_cols" = """
The sum of the three block widths, in the specification's own parenthesisation.
Five `usize` operations. O(1) by inspection."""

"quadeval::rlin_rows" = """
`dRows + (outerRows + (1 + (1 + innerRows)))`: four `usize` additions. O(1) by
inspection."""

"quadeval::unflatten" = """
A reshape: it copies every entry of the input into per-block vectors and does
no arithmetic at all. A criterion row would measure the allocator and the
memcpy, which is the "measures the harness, not the item" bar of the header.
Its correctness is pinned by `unflatten_inverts_flatten_blocks` in
`tests/quadeval_semantics.rs`, at a width where the block count and the block
width differ so a transposed reshape cannot pass.

Removal condition: none. A reshape does not become arithmetic at any
parameter."""

"quadeval::stack" = """
`w-hat ++ flatten(t-hat) ++ z-hat`, a concatenation with no arithmetic. At the
pinned parameters it copies `rlinCols = 57 344` `Rq` = 448 MiB, so a row here
would be a memcpy benchmark. Same class as `unflatten`; pinned by
`stack_and_unstack_are_inverse`."""

"quadeval::unstack" = """
The inverse split, likewise pure copying. Same class and same test."""

"quadeval::rlin_stmt" = """
The Eq. (20) block matrix, `rlinRows x rlinCols` = `5 x 57 344` `Rq` =
**2.2 GiB** at the pinned parameters -- the wall the target-4 brief records for
this matrix, and it is a *memory* wall, independent of `ring::mul`. Infeasible
even REDUCED: a shape small enough to hold is a shape where the block structure
no longer resembles Eq. (20).

Its arithmetic is measured through the pieces it calls, all of which have their
own rows: `gadget/gadget_transpose_mul` (c3, c4 and c5's blocks all go through
it) and `quadeval/tensor_g_matrix` (c5's tensor block). What a row here would
add over those is the zero padding and the copying, which is the excluded
class.

Removal condition: none at these parameters. A smaller `d` or `mu_0` would
change the object rather than make this measurable."""

"quadeval::PolyEvalStatement" = """
The composed chain's input statement: a four-field struct, its constructor and
its four accessors. O(1) by inspection, the `RlinStatement` class -- a criterion
run would measure the harness. Its content is measured wherever
`to_quad_eval_statement` is."""

"quadeval::to_quad_eval_statement" = """
Two `evalsplit::monomial_basis` calls, one per point half. Each builds `2^10`
entries as a product of `10` factors, so `2 * 1024 * 10 = 20 480` ring
products: **~29 s per iteration** at `ring/mul/1024 = 1.43 ms` measured
(`bench-report-20260908-t456-birth.json`). Excluded per the Fig. 9 policy
exception (header) until a sub-quadratic `ring::mul` lands -- and note the
honest arithmetic of that condition: Karatsuba's 2-4x leaves this at ~8 s,
still not a criterion row. What it actually needs is the ~30x an NTT would
give, which q = 2^32 - 99 forbids at radix 2 (NOTES.md § "The parameters forbid
a radix-2 NTT")."""

"sumcheck::nested_to_round_statement" = """
`zc_target_alpha` plus a move: the range target is the literal `0`, the
challenge prefix is `Vec::new()`, and the carried statement is moved rather
than copied, so the only work in the row is that one call. It is measured under
its own name by `zerocheck/zc_target_alpha` (the item belongs to `zerocheck`,
which is where its row lives), so a row here would time the same `n` rows
twice. The `honest_compute_y` precedent."""

"sumcheck::NestedZeroCheckStmt" = """
The zero-check statement carrier: five fields, a constructor, five accessors.
O(1) by inspection."""

"sumcheck::RoundMsg" = """
A pair of `UnivariatePoly` with a constructor and two accessors. O(1) by
inspection."""

"sumcheck::RoundStatement" = """
The per-round statement carrier: four fields, a constructor, four accessors.
O(1) by inspection. Note `round_out`, which *builds* one, IS benched -- through
`run_batched`, so the construction sits in setup rather than in the timed
region."""

"sumcheck::eq_free_factor" = """
Two `Ext4` coefficients: `(1 - t)` and `t + t - 1`. O(1) by inspection, the
`ring::len`/`ring::coeff` class of the header."""

"sumcheck::final_check" = """
Builds `alpha_public_table` and evaluates its multilinear extension, so at the
pinned `M_ZERO = 26` it inherits that table's `2^m0` wall (`67 108 864`
entries); the two `Ext4` comparisons and the `range_product` beside it are
free by comparison. The table itself has a REDUCED row since 2026-09-14
(`sumcheck/alpha_public_table` at `2^7` -- sized by its *genesis* variant, whose
frozen quadratic `c_eval_at` is re-measured every run; feasible at all only once
the champion's `c_eval_at` became linear); this composed check does not, because
nothing is left to measure once the table has a row: what `final_check` adds is
one `MultilinearEvals::eval`, one `eq_tilde`, two `Ext4` comparisons and a `u64`
compare, all free beside the `2^m0` table it builds.

Removal condition: the tensor split of `A~` (target-5 brief S6) landing as a
`perf-loop` accept, not a faster `ring::mul` -- this wall is `2^m0`."""

"sumcheck::honest_compute_y" = """
`zerocheck::w_table_mle_eval` at the sumcheck point, and nothing else -- the
specification's `honestComputeY` is literally that call
(`Sumcheck/FinalEval.lean:246`). Measured one level up by
`zerocheck/w_table_mle_eval`, so a row here would time the same work under a
second name and invite a `vs genesis` comparison between two rows that are the
same function. The `alpha_defect`/`h_alpha_evals` precedent."""

"sumcheck::round_loop" = """
`m0` rounds, each an `honest_compute_g` over the full folded tables plus two
`eval_mle_layer` folds, starting from `c_w_table_mle` and `alpha_public_table`.
So it carries both the `2^m0` table wall and `M_ZERO = 26` iterations of the
per-round cost: infeasible even REDUCED, and the sum of rows that are already
measured or already excluded individually.

Removal condition: the same tensor split as `alpha_public_table`, plus a
REDUCED-shape decision about what a whole-protocol row would even mean -- a
27-round loop at a toy cube measures the loop overhead, not the protocol."""

"sumcheck::honest_round_messages" = """
The prover's half of `round_loop`, and the same accounting: `m0` rounds of
`honest_compute_g` over the folded `2^m0` tables, starting from `c_w_table_mle`
and `alpha_public_table`. Both walls of `round_loop`, minus its `round_check`;
infeasible even REDUCED for the reason given there, and the sum of rows that
are measured individually. Same removal condition as `round_loop`."""

"sumcheck::round_verify_loop" = """
The verifier's half of `round_loop`: `m0` iterations of `round_check` then
`round_out` on a received message, no table and no witness. Both are benched
under their own names (`sumcheck/round_check`, `sumcheck/round_out`), and
`m0 = 26` at the widest, so a row here would time twenty-six of two rows that
already exist plus the loop -- the `round_loop` class of "the sum of rows
already measured", at a size where the loop overhead is the measurement."""

"zerocheck::below_two_pow" = """
O(m) by inspection and m is a *point-count exponent*: `m1 = 3` at the pinned
parameters, `m0 = 26` at the widest, so the loop body runs at most 26 times and
a criterion run would measure the harness rather than the item -- the
`ring::len`/`ring::coeff` class of the header, one step up. It is benched
transitively wherever `alpha_public_evals` and `zc_target_alpha` are.

Removal condition: none. This does not become measurable at any parameter the
specification admits; if it ever needs a row, that is a sign `m` stopped being
an exponent."""

"zerocheck::alpha_contract" = """
infeasible even REDUCED, and the arithmetic is the specification's own shape.
The double sum walks `(mu + n*delta) * d` table cells and `alphaContract`
(`ZeroCheck/Constraints.lean:540`) puts `M~_alpha(i, u)` *inside* the `ell`
loop, so every cell recomputes it -- and on a digit column that reaches
`ringswitch::c_eval_at_modulus`, which is the `O(d^2)` power-sum `eval2`
(`~5.2e5` `Ext4` multiplies at `d = 1024`). At the smallest witness with a digit
block at all (`mu = 1`, `n = 1`) that is `8 * 1024 = 8192` such calls, ~172 s
per criterion iteration; the real `(n, mu) = (5, 57344)` is astronomically
worse. Excluded per the Fig. 9 policy exception (header).

**Removal condition, and it is precise**: either hoist `M~_alpha` out of the
inner loop into the `n x mu` table the brief identifies
(the target-4 brief, cost model item 4), or collapse
`cEvalAt alpha Phi.phi` to `alpha^d + 1` (ten squarings; item 3 of that
brief's Corrections). Either alone brings this within criterion's reach, and
both are `perf-loop` candidates rather than translation choices. The semantics
are exercised on demand by `zerocheck_semantics::alpha_defect_vanishes_exactly_on_an_honest_lift`
and `..._is_the_double_sum_either_way_round`, both `#[ignore]`d and both run
green in release before the genesis freeze."""

"zerocheck::alpha_defect" = """
`alpha_contract` minus one `c_eval_at`, so it inherits that item's cost and its
exclusion verbatim -- the subtraction is one `Ext4` operation against ~172 s.
Same removal condition."""

"zerocheck::h_alpha_evals" = """
`alpha_defect` behind the `idx < n` padding guard: on a real row it is
`alpha_defect`'s cost, and above the rows it is free. Excluded for the real-row
half; the padding half is measured by nothing because there is nothing to
measure (one comparison). Same removal condition as `alpha_contract`. Its
padding semantics are pinned live by
`zerocheck_semantics::h_alpha_pads_above_the_real_rows`."""

"zerocheck::h_alpha" = """
`2^m_1` entries of `h_alpha_evals`, i.e. eight times that item's cost at the
pinned `M_ONE = 3`. Same removal condition as `alpha_contract`."""

"zerocheck::h_alpha_is_zero" = """
the same `2^m_1` scan as `h_alpha` plus one `Ext4::is_zero` per entry, and the
`H_alpha` verdict the verifier actually computes. Same removal condition as
`alpha_contract`. Note for whoever lifts that condition: this row's digest is
one `bool`, so it wants `check()`-style pinning in its bench file the way
`endpiece/lift_short_check` has, not just the digest oracle."""

"commit::generate_decomps" = """
per block: one `gadget_decompose` (8192 digit loops), one `A · s` (8192 ring
muls, ~25 s at d = 1024 schoolbook), one more decompose. Hours at the scheme's
`BLOCKS = 1024`; tens of seconds per iteration even at one block. Excluded per
the Fig. 9 policy exception (header) until a sub-quadratic `ring::mul` lands."""

"commit::commit_with_decomps" = """
`B · flatten(t̂)`: `BLOCKS · (INNER_ROWS · GADGET_DIGITS) = 8192` ring muls,
~25 s per iteration at d = 1024 schoolbook. Excluded per the Fig. 9 policy
exception (header) until a sub-quadratic `ring::mul` lands."""

"commit::derived_message" = """
per block: `gadget_mul` at `MESSAGE_ROWS = 1024` rows -- 8192 ring muls
(~25 s) per block, `BLOCKS = 1024` blocks. Excluded per the Fig. 9 policy
exception (header) until a sub-quadratic `ring::mul` lands."""

"commit::commit" = """
`generate_decomps` + `commit_with_decomps`: ~2^23 ring muls at the scheme's
shapes -- hours per criterion iteration. Excluded per the Fig. 9 policy
exception (header) until a sub-quadratic `ring::mul` lands."""

# --- quadeval: TWO different walls, and they must not be conflated ----------
#
# W4 (memory). The QuadEval witness `message` is `BLOCKS` blocks of
# `MESSAGE_ROWS · GADGET_DIGITS = 8192` ring elements, i.e. `2^23` elements of
# 8 KiB = **64 GiB** -- larger than this machine, and 8x the ~8 GiB undecomposed
# message. Nothing about `ring::mul` changes that: the removal condition is a
# blockwise/streaming witness API, **not** the multiplication champion. Every
# entry below marked W4 inherits that, and a sub-quadratic mul does NOT retire
# it.
#
# W1 (multiplication width). `relOut`'s three `1 x 8192` mat-vecs are 24 576 of
# its 28 672 ring muls (86%); those do retire with a mul champion, but the
# functions that carry them are W4-blocked first.

"quadeval::PublicParamsD" = """
a struct: two matrix fields, no computation. Types are excluded by
construction (`rustitems.benchable()` excludes `struct`), like every other
mirrored type in this crate."""

"quadeval::QuadEvalStatement" = """
a struct: four data fields, no computation. See `quadeval::PublicParamsD`."""

"quadeval::QuadEvalResponse" = """
a struct: three data fields, no computation. See `quadeval::PublicParamsD`."""

"quadeval::carrier" = """
W4 + W1: `blocks` x `carrier_entry`, i.e. `2^r · 2^m = 2^20` ring muls (~26 min
at d = 1024 schoolbook), over a `blocks x 8192` witness that is 64 GiB at
Fig. 9. `carrier_entry` itself is benched at a REDUCED row count."""

"quadeval::carrier_decomp" = """
W4: `carrier` plus `2^r · δ · d` digit steps. The digit layer is measured
directly as `gadget/balanced_gadget_decompose`; what cannot run here is the
64 GiB witness `carrier` reads."""

"quadeval::carrier_commit" = """
W4 + W1: `carrier_decomp` plus `dRows · (2^r · δ) = 8192` ring muls. Blocked by
the witness before the multiplication."""

"quadeval::honest_z" = """
W4 + W1, and the target's dominant term: `2^r · n = 2^23` full ring products
(`scalarVecMul` multiplies by a ring element, not a constant) -- the same order
as `commit::commit`, hours per criterion iteration -- over the 64 GiB witness.
Both walls fire; the memory one is not removed by a mul champion."""

"quadeval::honest_compute_v" = """
W4 + W1: `carrier_commit` under another name (`QuadEval/Reduction.lean`)."""

"quadeval::honest_compute_resp" = """
W4 + W1: `carrier_decomp` + `honest_z` + the bounded z decomposition,
~2^23 + 2^20 ring muls over the 64 GiB witness."""

"quadeval::j_mul" = """
a params-fixed wrapper over `gadget::gadget_mul_z` at the scheme's
`n = MESSAGE_ROWS · GADGET_DIGITS = 8192`, whose input `ẑ` alone is
`n · Z_DIGITS = 40960` ring elements = 320 MiB. The operation itself is
measured as `gadget/gadget_mul_z` at a REDUCED row count; a case here would
measure the 320 MiB corpus."""

"quadeval::rel_out" = """
W1 (and W4 through the witness that produces its response): 28 672 ring muls
per call, ~43 s per iteration, of which the three `1 x 8192` mat-vecs are 86%.
Retires with a sub-quadratic `ring::mul` champion -- unlike the W4 entries
above. Its cheap conjunct, the box test, is benched as `quadeval/in_sb`."""

"quadeval::paper_rel_out" = """
W1: `rel_out` with the box check in place of the ball; the same 28 672 ring
muls dominate. Its distinguishing conjunct is benched as
`quadeval/vec_in_sb`."""

# --- ring-switch representation types --------------------------------------

"ringswitch::QuotientRow" = """
a representation newtype around one coefficient row, with no computation of
its own. The `rhoAsRq` presentation change is separately measured as
`ringswitch/rho_as_rq`."""

"ringswitch::RlinStatement" = """
a type declaration, not code: the public matrix, right-hand side and `ℓ∞`
bound of `R^lin`, with no computation of its own. Its three accessors are
single-field reads and carry no `Mirrors` marker. The operations that read it
are measured -- `zerocheck/m_alpha_tilde_matrix`, `.../m_alpha_tilde_digit`,
`.../alpha_public_evals`, `.../zc_target_alpha`."""

"ringswitch::LiftedWitness" = """
a struct containing `z` and the quotient rows, with no computation. Its
consumers `lift_message`, `lift_commit`, and `lift_short_check` are measured."""

"sumcheck::round_node" = """
one `Fp::new` and one `Ext4::from_base` on a `usize` cast: straight-line, no
loop, no allocation. A case over it would time the `black_box` around two
constructor calls. It is measured at scale through `sumcheck/interpolate`,
which calls it `33 x 32` times per invocation, and its *values* are pinned in
that binary's `check()` -- nodes `0` and `1` explicitly, because those are the
two `roundCheck` evaluates (`Sumcheck/Rounds.lean:100`)."""

"ringswitch::long_mul" = """
private (`hachi/src/ringswitch.rs`): the unreduced `RING_DEGREE²` product that
`c_row_sum` sums over its `μ` columns -- `ring::mul` minus the negacyclic fold,
so its cost is `ring/mul`'s and its shape is measured through
`ringswitch/c_row_sum`, which calls it once per column. A case here would
restate `ring/mul` without the fold and report it as a new number."""

"ringswitch::div_by_modulus" = """
private (`hachi/src/ringswitch.rs`): one `O(N)` pass over a `2N - 1`-word vector
with one subtraction per slot, no products -- microseconds against the
milliseconds of the long products it follows. Measured through
`ringswitch/c_quotient`, which is exactly one row sum, one `yᵢ` subtraction and
one division; the division's share of that row is the `O(N)` term."""

"ringswitch::rho_digits_at" = """
one bounds-checked `Vec` index over the benched `ringswitch/rho_digits`
(`hachi/src/ringswitch.rs`): the body is `rho_digits(&rho[i].0, u)` and nothing
else, so a case here would restate `ringswitch/rho_digits` plus an index and
report the sum as a new number. It exists to address a digit by the pair
`(row, digit)` rather than by a flat index, which is the shape
`rhoDigitsShortCheck` has and is what keeps `rho_digits_short_check`'s extracted
model free of a `usize` product (NOTES.md § "The flat index the specification
does not have"); the pair form costs one index more than the flat one, which is
the only thing this item adds to what is already measured. Its consumer
`endpiece/rho_digits_short_check` is benched and walks it 40 times per call, so
a regression in it could not hide."""

"zerocheck::eq_weight" = """
one `m₁`-factor product with no loop over data: at the pinned
`params::M_ONE = 3` that is three `Ext4` multiplies and three bit tests, and the
factor count is `tau1.len()` rather than a data length. A case here would time
the `black_box` around three multiplies, and would land inside the
100 ns - 2 µs band the certified sweep found false verdicts in (NOTES.md § "The
certified null-slot sweep"). It is measured at scale through both of its
callers, `zerocheck/alpha_public_evals` and `zerocheck/zc_target_alpha`, each of
which calls it `n` times per invocation -- the same reasoning as
`commit::centered_abs` above, which is per-coefficient work measured through the
norms that call it."""

"commit::generate_decomps_balanced" = """
the balanced twin of `commit::generate_decomps`, and `ring::mul`-bound for the
same reason: per block one `balanced_gadget_decompose` (8192 digit loops), one
`A · s` (8192 ring muls, ~25 s at d = 1024 schoolbook), one more decompose. The
digit layer it differs in is a few percent of that, and is measured directly as
`gadget/balanced_gadget_decompose`. Excluded per the Fig. 9 policy exception
(header) until a sub-quadratic `ring::mul` lands."""

"commit::commit_balanced" = """
`generate_decomps_balanced` + `commit_with_decomps`: ~2^23 ring muls at the
scheme's shapes -- hours per criterion iteration, exactly as `commit::commit`.
Excluded per the Fig. 9 policy exception (header) until a sub-quadratic
`ring::mul` lands."""

"commit::verify_weak" = """
per block: the challenge scaling `c · sᵢ` alone is 8192 ring muls (~25 s).
Excluded per the Fig. 9 policy exception (header) until a sub-quadratic
`ring::mul` lands."""

"commit::verify" = """
`verify_weak` plus `derived_message` plus the per-block equality -- strictly
more than either excluded part. Excluded per the Fig. 9 policy exception
(header) until a sub-quadratic `ring::mul` lands."""

"evalsplit::MlPoly::to_matrix" = """
no arithmetic, but the shape is const-bound: `ML_POLY_LEN = 2^20` ring copies
of 1024 coefficients each -- an ~8 GiB corpus built per variant before a
single sample. Excluded per the Fig. 9 policy exception (header); returns with
witness streaming or a smaller committed-polynomial parameter set."""

"evalsplit::to_polynomial" = """
the inverse reshape of `to_matrix`, same const-bound `2^20`-element, ~8 GiB
shape. Excluded per the Fig. 9 policy exception (header)."""

"evalsplit::MlEvals::to_matrix_eval" = """
the value-vector reshape, same const-bound `2^20`-element, ~8 GiB shape as
`to_matrix`. Excluded per the Fig. 9 policy exception (header)."""

"evalsplit::MlPoly::eval_split" = """
reshape + two bases + `split_form`: the bilinear form alone is
`ML_LOW_LEN · ML_HIGH_LEN = 2^20` ring muls (~an hour at d = 1024 schoolbook),
on an ~8 GiB corpus. Excluded per the Fig. 9 policy exception (header) until a
sub-quadratic `ring::mul` lands."""

"evalsplit::MlEvals::eval_split_eval" = """
the Lagrange twin of `eval_split`, same `2^20` ring-mul bilinear form on the
same ~8 GiB shape. Excluded per the Fig. 9 policy exception (header) until a
sub-quadratic `ring::mul` lands."""

# --- end piece: the statement type and the two identities -------------------
#
# Target 6's `endPieceCheck` itself is benched (`endpiece/end_piece_check`,
# REDUCED, both walls stated at the case). What is excluded here is the record
# it reads and the two functions around it that ArkLib defines as identities.

"endpiece::WEvalStatement" = """
a type declaration, not code: the three-field record `(t, point, value)` the
sumcheck hands the end piece (`Sumcheck/FinalEval.lean:72`), with no
computation of its own. Its three accessors are single-field reads and carry
no `Mirrors` marker. The one operation that reads it, `end_piece_check`, is
benched as `endpiece/end_piece_check`."""

"endpiece::end_piece_prove" = """
the identity on a `LiftedWitness`, by value (`hachi/src/endpiece.rs`): the
body is `w`. `endPieceProver`'s single `sendMessage` is `fun w => pure (w, w)`
and its state before and after that round is the witness
(`EndPiece/Reduction.lean:241-252`), so there is no arithmetic to translate
and none to time: a criterion run would measure the `black_box` around the
argument and the move out of it, i.e. the harness. O(1) by inspection, the
`ring::Rq::coeff` class. Its cost as a *caller* pays it -- one move of two
`Vec` headers -- is what `endpiece/end_piece_check` pays to receive the same
witness by reference."""

"endpiece::end_piece_witness" = """
the identity on the transcript's one message, with the statement taken and
ignored (`hachi/src/endpiece.rs`): the body is `message`. `endPieceWitness
_stmt tr = tr 0` (`EndPiece/Reduction.lean:172-174`), and a `FullTranscript`
of the one-round `pSpecEndPiece` is that one message, so the function is a
move exactly as `end_piece_prove` is. O(1) by inspection; a criterion run
would time the harness, not the item."""
